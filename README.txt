Do a get on http://{address}:{port}/help

sqlite browser can be used to see what's inside the DB

Goal is to test the REST api for consistency and reliability, and performance for task get and post with 1000 then 10000 entries (total) on 20 then 100 projects.

The application is a todo manager, there are projects in which there are tasks.

run main.py to have a server.

